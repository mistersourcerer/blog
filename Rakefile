desc "Starts jekyll server in livereload moad."
task :serve do
  sh "bundle exec jekyll server --livereload"
end

desc "Includes drafts in the preview"
task :drafts do
  sh "bundle exec jekyll server --drafts --livereload"
end
